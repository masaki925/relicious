<%= stylesheet_link_tag "views/registration", :media => "all" %>

<%
  member = nil
  if params[:member]
    member = User.find(params[:member])
  end
%>

<h2>New meetup
  <% if member %>
    with <%= link_to image_tag( facebook_image_url(member.id, 'square') ), user_path(member) %>
  <% end %>
</h2>

<%= render 'form_new' %>

<%= link_to 'Back', meetups_path %>
