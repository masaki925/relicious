<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="keywords" content="Relicious, lunch" />
  <meta name="description" content="Relicious Connect to the foreign local over the dining table!" />

  <title>Relicious - log in / Sign in -</title>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <!--[if lt IE 9]>
    <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->

  <%= render 'layouts/google_analytics' if Rails.env.production? %>
</head>

<body id="login">

  <!-- header -->
  <% if current_user %>
    <% if current_user.locale and current_user.locale == "ja_JP" %>
      <%= render 'layouts/header-ja' %>
    <% else %>
      <%= render 'layouts/header-en' %>
    <% end %>
  <% end %>
  <!-- /header -->

  <div id="contents">
    <%= yield %>
  </div>

  <!-- footer -->
  <div class='footer' id="footer">
    <%= render 'layouts/footer' %>
  </div>
  <!-- /footer -->

  <%#= debug(params) if Rails.env.development? %>
</body>
</html>
