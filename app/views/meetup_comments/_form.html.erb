<%= form_for( [:meetup, @meetup_comment] ) do |f| %>
  <% if @meetup_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meetup_comment.errors.count, "error") %> prohibited this meetup_comment from being saved:</h2>

      <ul>
      <% @meetup_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :meetup %><br />
    <%= f.text_field :meetup %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
